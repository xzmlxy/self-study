要创建一个容器，首先需要下载一个镜像，然后在该镜像的基础上创建容器。
镜像并不是操作系统，但可以近似看作是缺少内核的操作系统环境，它需要使用到Host系统的内核才能运行。
镜像中的内容是只读的，而容器其实就是在镜像的基础上，创建了一层可写层，用于程序的安装、功能的发布等。
可以在一个镜像上创建多个容器，且它们是相互隔离的。用面向对象语言打个比方，镜像是面向对象中的“类”, 而容器是面向对象中的“实例”。
