scp（拷贝命令） -r（若拷贝文件夹，则拷贝文件夹下的所有文件） 原服务器用户名@原服务器IP:原地址 新地址

find（查找命令）-name 文件名

rm (删除命令) -r(可删除目录)

cp (拷贝命令)

grep (在文本文件中查找)  查找的字符串  指定目录(默认当前)

vim 文件名（进入vim打开文件）
在进入vim后默认为命令状态，按“:”自动跳至文末。此时可输入各种命令，如“q!”(不保存退出)、“wq”（保存退出）等等。
在命令状态下无法编辑文本，按“a”即可进入编辑模式，编辑完成后按ESC返回命令状态。
