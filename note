如果生成器函数有返回值，则该返回值会在迭代结束后的StopIteration异常中保存该值。
除非没有好坏之分，或者存在绝对合理的任何人无法反抗的分配机制，否则就会有争端，有争端就会有帮派，有帮派就会有领导者，进而就会有阶级。从客观世界来说不可能没有好坏之分；从社会来说不可能存在那样的制度，所以阶级永远存在于生物集体中。
executor.map返回的是一个生成器对象，用来迭代获取结果，并且获取结果的顺序与调用的顺序一致。由于生成器是可阻塞的，所以当前面的调用发生阻塞时，后面的调用即使已经结束，也必须在之后返回结果。
executor.submit和futures.as_completed这个组合比executor.map更灵活，因为submit方法能处理不同的可调用对象和参数，而 executor.map只能处理参数不同的同一个可调用对象。另外，结果的顺序主要由HTTP响应的时间长短来决定。
