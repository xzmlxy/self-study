在IEEE浮点数标准中，阶数代表了当前数所在的区间，表征了数的大小；而尾数会均分这个区间，因此尾数的长度表征了数的相对精确度。由

于尾数的长度是固定的，所以相精确度不会随数据（区间范围）的变小而降低，除非数字小到阶数已无法表示的程度。从传统的绝对精确度的

角度来看该标准：数据越小绝对精确度越高；数据变大绝对精确度降低；但对数据的总体影响程度是不变的——这显然是合理的做法。这也是

为什么越接近0的地方，可表示的数也越稠密（P.）。以(P.)爱国者导弹的问题为例，当前这种浮点数表示方式再结合向偶数舍入，在精确度

上会比定点表示法优秀好几个数量级。
